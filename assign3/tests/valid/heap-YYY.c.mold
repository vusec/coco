
#include <stdlib.h>
#include <stdio.h>
#include <string.h>

//PREFIX

int *x;

int main() {
    // RANDOM_CODE
    // RANDOM_CODE
    unsigned size = INT(12,20);
    x = malloc(size);
    memset(x, 0, size);
    // RANDOM_CODE
    // RANDOM_CODE
    printf("%d\n", (int)x[2]);
    // RANDOM_CODE
    free(x);
    // RANDOM_CODE
}