
#include <stdlib.h>
#include <stdio.h>
#include <string.h>

//PREFIX

int main() {
    // RANDOM_CODE
    // RANDOM_CODE
    unsigned size = 20;
    int *x = malloc(size);
    memset(x, 0, size);
    // RANDOM_CODE
    // RANDOM_CODE
    printf("%d\n", (int)x[INT(0,3)]);
    // RANDOM_CODE
    free(x);
    // RANDOM_CODE
}